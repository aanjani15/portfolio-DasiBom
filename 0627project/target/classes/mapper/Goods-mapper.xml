<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.booksajo.dasibom.service.dao.GoodsDAO">

    
    <select id="getOnegoods" resultType="com.booksajo.dasibom.vo.GoodsVO">
        SELECT goods_id, goods_name, category, price, image_path
        FROM goods_table
        WHERE goods_id = #{goods_Id}
    </select>

	<select id="getAllGoods" resultType="GoodsVO">
		SELECT goods_id, goods_name, category, price, image_path
        FROM goods_table
	</select>

	<!-- 굿즈 사진 리스트 조회 -->
	<select id="getGoodsPhotoList" parameterType="int" resultType="com.booksajo.dasibom.vo.PhotosVO">
	    SELECT photo_id AS photo_Id,
	           category_type,
	           post_id AS post_Id,
	           img_path
	    FROM photos_table
	    WHERE category_type = 'GOODS'
	      AND post_id = #{goods_Id}
	    ORDER BY photo_id ASC
	</select>
	
	<insert id="insertGoodsCart"
		parameterType="com.booksajo.dasibom.vo.GoodsWishlistVO">
		INSERT INTO goods_wishlist_table (user_id, goods_id, added_date,
		image_path, list_type, count)
		VALUES (#{user_Id}, #{goods_Id}, SYSDATE, 
		#{image_Path}, #{list_Type}, #{count})
	</insert>
	
	<select id = "checkExist" parameterType="com.booksajo.dasibom.vo.GoodsWishlistVO" resultType="int">
		SELECT COUNT(*)
		FROM goods_wishlist_table
		WHERE user_id = #{user_Id}
		AND goods_Id = #{goods_Id}
		AND list_type = #{type}
	</select>
	
	<insert id="insertGoodsWishlist"
		parameterType="com.booksajo.dasibom.vo.GoodsWishlistVO">
		INSERT INTO goods_wishlist_table (user_id, goods_id, added_date,
		image_path, list_type, count)
		VALUES (#{user_Id}, #{goods_Id}, SYSDATE, 
		#{image_Path}, #{list_Type}, #{count})
	</insert>
	
</mapper>